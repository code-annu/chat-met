<!DOCTYPE html>
<html
  lang="en"
  class="bg-gray-100 min-h-screen flex items-center justify-center p-4"
>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= user.username %> (<%= user.fullname %>)</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gradient-to-br from-gray-200 to-gray-300">
    <div
      class="bg-white dark:bg-gray-800 rounded-xl shadow-2xl max-w-sm w-full text-center overflow-hidden transition-shadow duration-300 hover:shadow-indigo-500/30"
    >
      <!-- Header / Gradient Background -->
      <div class="relative h-24 bg-gradient-to-r from-indigo-600 to-blue-600">
        <!-- Avatar overlay -->
        <img
          src="<%= user.avatarUrl || 'https://i.pravatar.cc/150?img=3' %>"
          alt="<%= user.fullname %>"
          class="absolute bottom-0 left-1/2 transform -translate-x-1/2 translate-y-1/2 w-24 h-24 rounded-full border-4 border-white dark:border-gray-800"
        />
      </div>
      <!-- Main content -->
      <div class="pt-16 pb-6 px-6">
        <h1 class="text-2xl font-semibold text-gray-800 dark:text-gray-100">
          <%= user.fullname %>
        </h1>
        <p class="text-gray-600 dark:text-gray-400 mb-4">
          @<%= user.username %>
        </p>
        <p class="text-sm text-gray-500 dark:text-gray-300 mb-6">
          Joined on <%= user.createdAt.toDateString() %>
        </p>
        <!-- Action buttons -->
        <div class="flex justify-center gap-4">
          <a
            href="/"
            class="px-4 py-2 bg-blue-500 text-white rounded-md font-medium hover:bg-blue-600 transition"
          >
            Home
          </a>
          <a
            href="/logout"
            class="px-4 py-2 bg-red-500 text-white rounded-md font-medium hover:bg-red-600 transition"
          >
            Logout
          </a>
        </div>
      </div>
    </div>
    <script>
      // Automatically enable dark mode if preferred
      if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
        document.documentElement.classList.add("dark");
      }
    </script>
  </body>
</html>
